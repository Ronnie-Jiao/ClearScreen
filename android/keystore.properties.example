# Copy this file to android/keystore.properties and replace every value.
# The real file is ignored by Git. Keep the keystore and passwords backed up
# outside the repository; all production updates must use the same certificate.
storeFile=clearscreen-release.keystore
storePassword=REPLACE_WITH_STORE_PASSWORD
keyAlias=clearscreen
keyPassword=REPLACE_WITH_KEY_PASSWORD
